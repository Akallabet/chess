{
  "name": "@chess/chess",
  "version": "1.0.1",
  "description": "",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "tap --rcfile=test/config/tap.yml",
    "test:watch": "tap --watch --rcfile=test/config/tap.yml",
    "test:only": "tap --only --rcfile=test/config/tap.yml",
    "emit:types": "tsc -p . --emitDeclarationOnly",
    "bundle": "swc src -d dist -q",
    "performances": "node ./scripts/performances.js",
    "prebuild": "rm -rf dist",
    "build": "npm run bundle",
    "postbuild": "npm run emit:types"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.56",
    "@typescript-eslint/eslint-plugin": "^5.47.1",
    "@typescript-eslint/parser": "^5.47.1",
    "eslint": "^8.30.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-import": "^2.26.0",
    "prettier": "^2.8.1",
    "prettier-plugin-tailwindcss": "^0.2.1",
    "ramda": "^0.28.0",
    "tap": "^16.3.2",
    "tsx": "^3.12.6",
    "typescript": "^4.9.4"
  }
}
